[build]
  command = "npm install && npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "18"  # ИЗМЕНИЛ на 18
  NPM_FLAGS = "--force"

[build.processing]
  skip_processing = true
